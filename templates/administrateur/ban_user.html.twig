{% extends 'base.html.twig' %}

{% block title %} Prékar {% endblock %}

{% block body %}

<div class="flex flex-col">
    {% for user in users %}
        <div class="flex flex-row gap-4" data-feur="{{ user.id }}">
            <div>{{ user.email }}</div>
            <div>{{ user.nom }}</div>
            <div data-user="status" >{{ user.statut }}</div>
            <div><div data-id="{{ user.id }}" class="btn-rainbow hover:cursor-pointer">
                    BOUTON
                </div></div>
        </div>
    {% endfor %}
</div>
    
{% endblock %}

{% block javascripts %}
{{ encore_entry_script_tags('app_admin') }}
{% endblock %}